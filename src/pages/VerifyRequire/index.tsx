import { LayoutFooter } from '../../components/layout/LayoutFooter';

export const VerifyRequire: React.FC = () => {

  return (
    <LayoutFooter>
      <h1>No no no no</h1>
    </LayoutFooter>
  );
};
