import { FieldSpecifier } from './types';

/**
 * Apenas um especificador de onde estará o campo Field original.
 * @returns Um elemento null, totalmente inutilizável para a renderização.
 */
export const FieldSpecificer: React.FC<FieldSpecifier> = () => null;
