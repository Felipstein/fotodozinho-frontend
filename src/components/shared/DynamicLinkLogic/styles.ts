import { Link } from 'react-router-dom';
import styled from 'styled-components';

export const ResetLinkStyled = styled(Link)`
  color: inherit;
  background-color: inherit;
  padding: inherit;
  margin: inherit;
  border-radius: inherit;
  font-size: inherit;
  font-weight: inherit;
  text-decoration: inherit;
`;
